# 实验设计：中文文档 RAG 的解析式 vs 视觉式

**日期** 2026-09-23 · **状态** 执行中 · **前置** [MVP 结果](#附录mvp-结果与它暴露的问题)

---

## 一、要回答的问题

> 中文文档检索，把页面**解析成文字**和**直接当图编码**，哪种更好？在什么类型的页面上好？差距有多大？

MVP（31 页、15 条查询）给出的方向是视觉式略优，但那个规模下无法区分信号与噪声。本轮把规模提到能做统计判断的量级。

## 二、语料

| 项 | 规格 |
|---|---|
| 文档 | 40 份 2024 年年度报告，来自巨潮资讯网 |
| 板块 | 沪主板 / 深主板 / 创业板 / 科创板 各 10 家 |
| 页面 | 每份取 **2 段连续 30 页**，共 **2400 页** |
| 候选池 | 全部 2400 页混在一起检索（不按文档分组） |

**为什么取连续页段而不是随机抽页**：跨页表格是本项目的核心考察对象，随机抽页会把这个现象破坏掉。页段按文档长度的比例选取（约 15% 处和 50% 处），前者多为经营讨论与管理层分析，后者多为财务报表区，版式差异大。

**为什么用板块代理行业**：巨潮接口没有行业字段，板块是能拿到的最接近「不同版式传统」的代理变量。

## 三、查询集（决定结论可信度的地方）

### 生成偏差必须被测量，而不是被忽略

用 VLM 看着**页面图**生成问题，问题会带上版面线索，天然偏向视觉式；用**解析文本**生成，会偏向解析式。这是这类对比最容易被质疑的地方。

**做法：双源各 200 条，分开报指标。**

| 源 | 生成方式 | 预期偏差 |
|---|---|---|
| 视觉源 | Qwen2.5-VL-7B 看页面图出题 | 偏向视觉式 |
| 文本源 | 同一模型看 DeepDoc 解析文本出题 | 偏向解析式 |

两组的差值本身就是一个结果：**如果两条路线的优劣在两组上方向一致，结论才稳**；如果方向相反，说明差距主要来自出题偏差，而不是检索能力。

### 过滤规则（照搬 receipt-vlm 验证过的方法论）

1. **门控**：只对确实含可问内容的页出题，空白页/纯目录页跳过
2. **接地校验**：答案必须能在该页原文中定位（最佳子串相似度 ≥ 0.7），否则丢弃
   - 教训来源：receipt-vlm 实测约 12% 的 LLM 输出是**推断而非抽取**（票面只印「山东高速」，模型补成「山东高速集团有限公司」）
3. **唯一性检查**：查询须含可区分实体（公司名/年份/指标名）。若答案串在多页出现，把**所有匹配页记为真值**，而不是只记一页
4. **人工抽验 50 条**，估计残余噪声率，写进结果

## 四、对比系统

MVP 暴露出「视觉式」这个词太笼统——ColQwen2.5 的多向量 MaxSim 和单向量图像编码是两种东西。本轮拆开：

| | 系统 | 表示 | 模型 | 作用 |
|---|---|---|---|---|
| A | 解析式·稠密 | DeepDoc 文本 | Qwen3-VL-Emb-2B | 与 D 构成**受控对** |
| B | 解析式·词法 | DeepDoc 文本 | BM25 | RAGFlow 实际是混合检索，漏了不公平 |
| C | 解析式·混合 | DeepDoc 文本 | A+B 融合 | 解析式的真实上限 |
| D | 视觉式·单向量 | 页面图 | Qwen3-VL-Emb-2B | 与 A 构成**受控对** |
| E | 视觉式·多向量 | 页面图 | ColQwen2.5 MaxSim | 视觉式的真实上限 |

**两个关键对比**：

- **A vs D**：同一套权重、同样输入内容、唯一变量是表示形式 → 回答「表示方式本身的影响」
- **C vs E**：两条路线各自用最强配置 → 回答「实际选型该选哪条」

### 消融：切块策略

解析式的结果对切块敏感（MVP 里第 55 页整张表是一个 2139 字符的块，具体行信息被稀释）。增加一组 **A′ = 按表格行切块**，测这个可调参数能挽回多少差距。

## 五、指标与统计

- **检索质量**：nDCG@5、Recall@1、Recall@5、MRR，**按页面类型分层报**
- **分层方式**：用 DeepDoc 的版面识别输出自动标注（纯文字 / 含普通表格 / 含复杂表格 / 含图表），客观且可复现
- **成本**：建索引耗时、索引体积、单次查询延迟——多向量的代价必须量化，不能只报精度
- **统计**：per-query nDCG 的**配对自助法置信区间**，对 A-vs-D 和 C-vs-E 给出 p 值
  - MVP 那个 +0.065 到底是不是噪声，只有这一步能回答

## 六、计算预算（基于实测速率，非估计）

| 环节 | 实测速率 | 2400 页 |
|---|---|---|
| 解析（DeepDoc，4×4090） | 9.1 s/页 | **6.1 h** ← 唯一瓶颈 |
| 渲染页面图 | 0.23 s/页 | 9 min |
| 视觉编码（Qwen3-VL-Emb） | 0.12 s/页 | 5 min |
| 视觉编码（ColQwen2.5） | 0.21 s/页 | 8 min |
| 文本编码 | 0.015 s/块 | 约 2 min |
| 查询生成（Qwen2.5-VL-7B 本地） | 待测 | 约 1～2 h |

## 七、终止条件（写死，不许事后放宽）

- **视觉式在复杂表格上显著赢**（配对检验 p < 0.05）→ 结论成立，进阶段 1 微调
- **两者相当**（置信区间跨 0）→ 结论本身有价值：省下所有人的试错，项目转为「中文文档 RAG 选型实证」
- **解析式赢** → 说明中文场景下视觉路线不成立，同样是结论
- **双源结果方向相反** → 说明差距主要来自出题偏差，必须重做查询集，不许挑对自己有利的那组报

---

## 附录：MVP 结果与它暴露的问题

31 页候选池、15 条人工查询的结果：

| | 解析式 | 视觉式 | 差 |
|---|---|---|---|
| 未受控（BGE-m3 vs ColQwen2.5） | 0.892 | 1.000 | 0.108 |
| **受控（同为 Qwen3-VL-Emb-2B）** | **0.901** | **0.967** | **0.065** |

**原差距的约 40% 来自模型差异而非表示方式。** 解析式换模型几乎没变（0.892→0.901），视觉式换成单向量后掉了（1.000→0.967）。

分层上，差距集中在**跨页三级表头**（0.754 vs 0.875）和**普通表格**（0.875 vs 1.000），**纯文字和多级表头两层完全打平**。

### 已确认的机制（与原假设不同）

原以为「表格被拆烂 → 关键词丢失 → 检索不到」。实测**关键词没丢**：`COD`、`氨氮` 在 50-60 页九个页面上都在。

真正的机制是：**跨页表格让连续多页的文本高度雷同**，加上整张表被切成一个 2139 字符的大块，单向量被稀释，具体是哪家公司哪一行反而淹没了。视觉式赢在页面图保留了「这一页有三级表头」这个版面级信号。

这个发现直接导出了本轮的切块消融组 A′。
